name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test-and-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # TODO: replace placeholders below with the project's real commands.
      - name: Install dependencies
        run: echo "TODO: install deps (e.g. 'uv sync' or 'pip install -e .[dev]')"

      - name: Lint
        run: echo "TODO: lint (e.g. 'ruff check .')"

      - name: Format check
        run: echo "TODO: format check (e.g. 'ruff format --check .')"

      - name: Test
        run: echo "TODO: run tests (e.g. 'pytest -q')"
