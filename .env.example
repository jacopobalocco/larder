# Environment variable template.
# Copy to `.env` (gitignored) and fill in real values locally.
# Never commit real secrets. No environment variables were detected yet.
#
# Example:
# DATABASE_URL=
# API_KEY=
